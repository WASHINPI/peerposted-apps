import React, { Component } from 'react';
import { Text } from 'react-native';

class Settings extends Component {
    render() {
        return (<Text>Wellcom to traveler page</Text>);
    }
}

export default Settings;
